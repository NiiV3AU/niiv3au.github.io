document.addEventListener("DOMContentLoaded",()=>{const copyButtons=document.querySelectorAll(".copy-btn");copyButtons.forEach((button)=>{const buttonTextSpan=button.querySelector(".button-text");if(!buttonTextSpan){console.error('Konnte ".button-text" nicht im Button finden:',button);return}const originalText=buttonTextSpan.textContent;button.addEventListener("click",(event)=>{event.preventDefault();const textToCopy=button.dataset.clipboardText;if(textToCopy&&navigator.clipboard){navigator.clipboard.writeText(textToCopy).then(()=>{buttonTextSpan.textContent="Copied!";button.classList.add("copied-success");setTimeout(()=>{buttonTextSpan.textContent=originalText;button.classList.remove("copied-success")},1500)}).catch((err)=>{console.error("Fehler beim Kopieren in die Zwischenablage:",err);const errorTextSpan=button.querySelector(".button-text");if(errorTextSpan){const originalColor=errorTextSpan.style.color;errorTextSpan.textContent="Error!";errorTextSpan.style.color="red";setTimeout(()=>{errorTextSpan.textContent=originalText;errorTextSpan.style.color=originalColor},2000)}})}else{console.warn("Kein Text zum Kopieren gefunden oder Clipboard API nicht verfügbar.")}})})});
